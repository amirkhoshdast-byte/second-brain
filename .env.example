# =============================================================
# .env.example — متغیرهای محیطی پلتفرم هوش سازمانی
# =============================================================
# کپی این فایل به .env و مقادیر را تغییر دهید:
#   cp .env.example .env
# هرگز فایل .env را به git commit نکنید.
# =============================================================

# -------------------------------------------------------------
# PostgreSQL
# -------------------------------------------------------------
POSTGRES_DB=orgplatform
POSTGRES_USER=orgplatform
POSTGRES_PASSWORD=changeme-postgres-STRONG

# -------------------------------------------------------------
# Qdrant
# -------------------------------------------------------------
QDRANT_API_KEY=changeme-qdrant-STRONG

# -------------------------------------------------------------
# n8n
# -------------------------------------------------------------
N8N_USER=admin
N8N_PASSWORD=changeme-n8n-STRONG

# -------------------------------------------------------------
# Redis
# -------------------------------------------------------------
REDIS_PASSWORD=changeme-redis-STRONG

# -------------------------------------------------------------
# Open WebUI
# -------------------------------------------------------------
WEBUI_SECRET_KEY=changeme-webui-STRONG-32chars-minimum

# -------------------------------------------------------------
# LLM (Ollama — Native روی macOS)
# -------------------------------------------------------------
# Ollama روی پورت پیش‌فرض اجرا می‌شود:
OLLAMA_HOST=http://localhost:11434
# مدل پیش‌فرض برای تست:
OLLAMA_DEFAULT_MODEL=qwen3:8b
# مدل embedding:
EMBEDDING_MODEL=BAAI/bge-m3

# -------------------------------------------------------------
# محیط
# -------------------------------------------------------------
ENVIRONMENT=development
LOG_LEVEL=info
