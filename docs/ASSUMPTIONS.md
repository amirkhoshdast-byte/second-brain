# ASSUMPTIONS.md — فرض‌های صریح و سؤالات بی‌پاسخ

> **هدف این فایل:** هر فرضی که در نبود اطلاعات قطعی درباره BPMS، مقیاس سازمانی یا محدودیت‌های اجرایی گرفته می‌شود، باید اینجا صریح ثبت شود — نه در دل مستندات فنی به‌عنوان واقعیت. طبق قاعده Cowork: «درباره BPMS فرض نگیر؛ نقطه اتصال را انتزاعی طراحی کن و فرض را اینجا بنویس.»

## وضعیت
اسکلت WP-1. محتوا در WP-2 تا WP-8 که فرض جدیدی لازم شود، اینجا اضافه می‌شود.

## سؤالات بلوکه‌کننده (بخش ۱۲ سند اجرایی) — پاسخ‌شان با کاربر است

| # | سؤال | وضعیت |
|---|---|---|
| 1 | BPMS کدام محصول است؛ دسترسی خواندن به پایگاه‌داده‌اش هست؟ | TBD |
| 2 | چند واحد و نمایندگی، با چه تناوب گزارش‌دهی؟ | TBD |
| 3 | اختیار تغییر فرم گزارش در BPMS هست یا نیازمند مصوبه است؟ | TBD |
| 4 | بالاترین سطح طبقه‌بندی داده‌ای که وارد سیستم می‌شود؟ | TBD |
| 5 | اسپانسر مدیریتی پروژه کیست؟ | TBD |
| 6 | بودجه سخت‌افزار سال آینده مصوب است؟ | TBD |

این‌ها پیش‌نیاز **فاز اجرا** هستند، نه فاز مستندسازی فعلی؛ اسپرینت اول بدون پاسخ آنها هم قابل تکمیل است چون هیچ اتصال واقعی به BPMS در دامنه این اسپرینت نیست.

## فرض‌های فنی گرفته‌شده تا این لحظه

### WP-3 — فرم گزارش استاندارد

| کد | فرض | تأثیر اگر غلط باشد |
|---|---|---|
| A-1 | BPMS توانایی تعریف فیلد ساختارمند و enum را دارد | اگر نه: فرم موازی سبک خارج از BPMS ضروری می‌شود (مسیر کوتاه‌شده بخش ۴.۲ سند اجرایی) |
| A-2 | SSO/LDAP برای احراز هویت کاربر در دسترس است | اگر نه: جدول user_id مستقل لازم است |
| A-3 | ~~دوره گزارش‌دهی فصلی است~~ → **ماهانه** (تأیید کاربر، ۱۴۰۵/۰۷/۰۳) | حجم رکوردها ۳× حالت فصلی؛ `period_id` با grain ماه (`1405-M07`) |
| A-4 | تعداد واحدهای موظف به گزارش ۲۰ تا ۵۰ است | TBD — سؤال #2 |
| A-5 | یک واحد یک کاربر ارسال‌کننده اصلی دارد | اگر چندنفره: delegation workflow لازم است |

### محصول B — قالب Word و ارزیابی ماهانه

| کد | فرض | تأثیر اگر غلط باشد |
|---|---|---|
| A-6 | BPMS فایل Word را بدون تبدیل و با Content Controlهای سالم منتقل می‌کند | پارس از روی جدول‌ها/Bookmark با دقت کمتر؛ بازبینی انسانی بیشتر |
| A-7 | BPMS پیوست‌ها را جدا از Word تحویل می‌دهد | پیوست‌ها داخل Word جاسازی و استخراج می‌شوند |
| A-8 | متادیتای BPMS (فرستنده، زمان، شماره پیگیری) همراه فایل قابل دریافت است | از Header داخل Word خوانده می‌شود و قابل جعل‌تر است |
| A-9 | وزن‌های ارزیابی `EV-0.1` (۴۰/۲۵/۱۵/۱۰/۱۰) پیش از کالیبراسیون قابل قبول است | بازبینی وزن‌ها پس از ۳ ماه پایلوت — `EVALUATION_MODEL.md` §۷ |
| A-10 | مهلت ارسال روز ۵ و بستن دوره روز ۱۰ ماه شمسی است | تقویم `BPMS_INTEGRATION.md` §۵ جابه‌جا می‌شود |
| A-11 | رتبه‌بندی فقط پس از ۳ دوره داده و درون گروه هم‌تیپ (`unit_type`) مجاز است | اگر اسپانسر رتبه‌بندی فوری بخواهد: امتیاز کالیبره‌نشده منتشر می‌شود — ریسک مقاومت واحدها |
| A-12 | همه پردازش AI روی Ollama محلی انجام می‌شود | اگر مدل ابری مجاز شود: فقط برای داده `public`/`internal` |
