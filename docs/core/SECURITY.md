# SECURITY.md — امنیت، هویت و دسترسی

> **هدف این فایل:** SSO، مدل نقش‌ها، سطوح دسترسی، طبقه‌بندی محرمانگی، قاعده «فیلتر دسترسی قبل از بازیابی RAG»، ممنوعیت اتصال مستقیم محصولات به مدل زبانی (الزام عبور از LLM Gateway).

## وضعیت
اسکلت WP-1 — محتوای تفصیلی در فاز بعد از این اسپرینت (خارج از دامنه WP-1 تا WP-8) یا در صورت نیاز به‌عنوان بسته کاری اضافه تعریف می‌شود.
