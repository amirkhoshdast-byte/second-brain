# DDL محصول B

> **هدف این پوشه:** DDL کامل PostgreSQL برای مدل ابعادی محصول B، در سه Schema جدا: `landing.sql` (خام)، `validated.sql` (پس از دروازه کیفیت)، `warehouse.sql` (مدل ابعادی نهایی).

## وضعیت
اسکلت WP-1 — محتوا در WP-5.
